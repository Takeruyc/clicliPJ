<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.VideoMapper">

    <insert id="insert">
        INSERT INTO video(
            title, category, cover_path, video_path, description, created_at
        ) VALUES(
            #{title}, #{category}, #{coverPath}, #{videoPath}, #{description}, #{createdAt}
        )
    </insert>

    <select id="findAll" resultType="com.example.demo.model.Video">
        SELECT * FROM video ORDER BY id DESC
    </select>

    <select id="findById" resultType="com.example.demo.model.Video">
        SELECT * FROM video WHERE id = #{id}
    </select>

    <select id="findByCategory" resultType="com.example.demo.model.Video">
        SELECT * FROM video WHERE category = #{category} ORDER BY id DESC
    </select>

    <delete id="delete">
        DELETE FROM video WHERE id = #{id}
    </delete>

</mapper>