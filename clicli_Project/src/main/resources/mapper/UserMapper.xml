<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.UserMapper">

    <insert id="insertUser" parameterType="com.example.demo.model.User">
        INSERT INTO `user` (
            id, nickname, email, password, role, created_at
        ) VALUES (
            #{id}, #{nickname}, #{email}, #{password}, #{role}, #{createdAt}
        )
    </insert>
    
    <select id="findByEmail" resultType="com.example.demo.model.User">
        SELECT
            id, nickname, email, password, role,
            created_at AS createdAt
        FROM `user`
        WHERE email = #{email}
    </select>
    
    <select id="existsByEmail" resultType="int">
        SELECT COUNT(*) FROM `user` WHERE email = #{email}
    </select>
    
    <select id="findAll" resultType="com.example.demo.model.User">
        SELECT * FROM `user` ORDER BY created_at DESC
    </select>

</mapper>